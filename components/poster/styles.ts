import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  poster: {
    width: 85,
    height: 120,
    borderRadius: 8,
    marginRight: 10,
    backgroundColor: "#ccc",
  },
});

export default styles;